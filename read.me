
FastAPI → servidor web principal

uvicorn[standard] → servidor ASGI

sqlalchemy[asyncio] → ORM assíncrono

asyncpg → driver PostgreSQL assíncrono

alembic → migrações de banco

pydantic → validação de dados

python-jose[cryptography] → JWT/token e criptografia

passlib[bcrypt] → hashing de senhas

python-dotenv → ler variáveis do .env

jinja2 → renderizar HTML dinâmico (template engine)